# Next

## Project

- add linting, fix styles, add rules from reference_clones/totvibe-ocr
- workspace catalog
- user story tests
- ci gate, repo settings, rename master -> main
- board, issues
- knip, ncu
- justfile with test -> typecheck -> lint -> knip cascade

## Features

- writing is currently blocked by the gate
- add debug pane so that I could see logs/errors/etc.
- add logging
- slash commands to allow autocomplete
- web search tool
- resume session feature
- display file edits with Diff, display Code, markdown
- speak in Markdown (toggle)
- gate destructive commands (Auto mode)
- auto-worktree if attempt to edit on main
- connect gemini 3.5 flash
- store API keys more secure
- refactor UI, use jotai

## Features long term

- support web UI
- support Discord
- deploy to VPS
- speak in Markdown with mermaid diagrams (use either markdown renderer, or render to image)
- allow multiple users running on VPS
- enhance existing read_file and write_file tools

## Rest (not today)

- publish on npm
- polish visuals, controls, indicators etc. (check pi, OpenCode, OpenClaw, Codex, etc.)
- show Animations
- run on mobile (if Discord not enough)
