# Coding-Agent Feature Matrix

A source-code-derived comparison of seven coding agents found in this directory. Every agent was reviewed against an identical 58-feature taxonomy and scored **0–100** based **only on source-code evidence** (not marketing/README claims). Scores: `0` = absent, `1–25` = stub/partial, `26–50` = basic, `51–75` = solid, `76–90` = comprehensive, `91–100` = best-in-class.

**Agents under review**

| Key | Agent | Language / Stack | One-line identity |
|-----|-------|------------------|-------------------|
| aider | **aider** | Python (LiteLLM) | Terminal pair-programmer; repo-map + SEARCH/REPLACE + deep git |
| claude-code | **claude-code** | TypeScript / Bun (Ink TUI) | Anthropic-only agent with best-in-class hooks/skills/plugins/safety |
| hermes | **hermes-agent** | Python | Huge multi-channel assistant; LSP, cron, skills, browser/computer-use |
| kilocode | **kilocode** | TypeScript / Bun (Effect-TS, OpenCode fork) | CLI + VS Code/JetBrains/Zed; vector RAG, worktree Agent Manager |
| openclaw | **openclaw** | JS/TS (gateway + 131 plugins) | Plugin-centric multi-channel platform; voice, media-gen, desktop apps |
| opencode | **opencode** | Go (Bubble Tea TUI) | Clean Go terminal agent; LSP diagnostics, permissions, SQLite |
| pi | **pi** | TypeScript monorepo | Minimalist-by-design; rich provider/streaming layer + extension ecosystem |

> Feature descriptions are listed in the **[Feature Descriptions](#feature-descriptions)** section after the tables. The tables below use feature names only.

---

## Table 1 — Implementation Scores (0–100)

| # | Feature | aider | claude-code | hermes | kilocode | openclaw | opencode | pi |
|---|---------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 1 | Multi-provider LLM support | 90 | 45 | 90 | 95 | 92 | 90 | 95 |
| 2 | Local / self-hosted models | 78 | 5 | 80 | 70 | 80 | 70 | 70 |
| 3 | Model routing / per-role models | 88 | 80 | 85 | 78 | 72 | 80 | 60 |
| 4 | Prompt caching | 82 | 95 | 92 | 92 | 95 | 78 | 90 |
| 5 | Reasoning / extended-thinking | 80 | 90 | 88 | 88 | 88 | 72 | 92 |
| 6 | Streaming responses | 75 | 90 | 85 | 90 | 90 | 88 | 95 |
| 7 | Search/replace (diff) editing | 92 | 80 | 80 | 90 | 85 | 80 | 90 |
| 8 | Whole-file editing | 85 | 75 | 78 | 80 | 80 | 75 | 80 |
| 9 | Unified-diff editing | 80 | 10 | 45 | 78 | 55 | 60 | 30 |
| 10 | Multi-file coordinated edits | 80 | 70 | 80 | 75 | 80 | 78 | 55 |
| 11 | Edit validation & auto-repair | 88 | 80 | 82 | 85 | 70 | 70 | 45 |
| 12 | Fuzzy / flexible match application | 90 | 65 | 90 | 92 | 82 | 45 | 85 |
| 13 | Repo map / codebase indexing | 95 | 55 | 35 | 85 | 45 | 25 | 20 |
| 14 | Tree-sitter / AST parsing | 90 | 60 | 15 | 80 | 5 | 5 | 0 |
| 15 | Semantic / embedding search (RAG) | 30 | 0 | 10 | 88 | 60 | 0 | 0 |
| 16 | Grep / regex / glob search | 35 | 90 | 80 | 88 | 70 | 80 | 88 |
| 17 | LSP integration | 0 | 80 | 90 | 90 | 78 | 82 | 0 |
| 18 | Shell / command execution | 78 | 95 | 85 | 90 | 90 | 80 | 90 |
| 19 | Tool / function-calling framework | 25 | 90 | 88 | 90 | 88 | 78 | 92 |
| 20 | MCP client | 0 | 90 | 90 | 90 | 90 | 75 | 0 |
| 21 | MCP server | 0 | 85 | 88 | 15 | 82 | 0 | 0 |
| 22 | Web search | 0 | 80 | 90 | 80 | 85 | 0 | 0 |
| 23 | Web / URL fetch & scrape | 70 | 80 | 85 | 78 | 85 | 70 | 0 |
| 24 | Browser automation | 10 | 70 | 92 | 55 | 80 | 0 | 0 |
| 25 | Image / multimodal input | 75 | 80 | 80 | 75 | 82 | 75 | 80 |
| 26 | Image / media generation | 0 | 0 | 85 | 0 | 85 | 0 | 55 |
| 27 | Autonomous agentic loop | 45 | 95 | 85 | 92 | 90 | 85 | 90 |
| 28 | Planning / plan mode | 70 | 85 | 55 | 85 | 65 | 10 | 5 |
| 29 | Sub-agents / task delegation | 25 | 90 | 88 | 85 | 90 | 70 | 5 |
| 30 | Todo / task-list tracking | 0 | 80 | 70 | 80 | 60 | 0 | 5 |
| 31 | Custom agent modes / personas | 60 | 80 | 75 | 88 | 80 | 35 | 70 |
| 32 | Scheduled / background / cron tasks | 0 | 85 | 90 | 40 | 88 | 0 | 0 |
| 33 | Context compaction / token mgmt | 65 | 90 | 92 | 88 | 92 | 72 | 90 |
| 34 | Conversation persistence & history | 70 | 90 | 85 | 90 | 85 | 80 | 88 |
| 35 | Session resume / restore | 55 | 90 | 85 | 88 | 80 | 75 | 85 |
| 36 | Cost / token usage tracking | 85 | 95 | 85 | 88 | 82 | 80 | 70 |
| 37 | Long-term / cross-session memory | 10 | 90 | 82 | 70 | 85 | 30 | 10 |
| 38 | Git auto-commit / integration | 92 | 80 | 60 | 78 | 35 | 25 | 10 |
| 39 | Undo / change review / diff | 85 | 85 | 80 | 90 | 70 | 70 | 75 |
| 40 | PR / code-review automation | 5 | 80 | 65 | 70 | 55 | 20 | 40 |
| 41 | CLI / headless mode | 80 | 90 | 90 | 90 | 88 | 75 | 90 |
| 42 | Interactive TUI | 78 | 95 | 90 | 90 | 85 | 90 | 95 |
| 43 | IDE / editor extension | 15 | 75 | 60 | 92 | 70 | 0 | 0 |
| 44 | Desktop GUI app | 0 | 10 | 5 | 10 | 80 | 0 | 0 |
| 45 | Web UI / server / API | 55 | 80 | 88 | 85 | 85 | 0 | 5 |
| 46 | Voice input / speech / TTS | 70 | 85 | 88 | 0 | 90 | 0 | 0 |
| 47 | Custom slash commands | 80 | 90 | 80 | 88 | 80 | 80 | 90 |
| 48 | Hooks / lifecycle events | 5 | 95 | 85 | 85 | 85 | 5 | 95 |
| 49 | Plugin / extension ecosystem | 5 | 90 | 90 | 82 | 95 | 10 | 92 |
| 50 | Skills system | 0 | 95 | 92 | 85 | 90 | 0 | 90 |
| 51 | Project rules / context files | 65 | 90 | 88 | 90 | 85 | 75 | 85 |
| 52 | SDK / programmatic API | 60 | 90 | 80 | 88 | 78 | 20 | 95 |
| 53 | Permission / approval system | 70 | 95 | 90 | 90 | 90 | 85 | 15 |
| 54 | Sandboxing / isolation | 5 | 90 | 90 | 35 | 92 | 30 | 10 |
| 55 | Security review / scanning | 0 | 75 | 85 | 55 | 80 | 5 | 0 |
| 56 | Test / lint runner integration | 85 | 75 | 70 | 70 | 65 | 25 | 5 |
| 57 | Telemetry / analytics | 70 | 90 | 75 | 88 | 75 | 0 | 20 |
| 58 | Internationalization (i18n) | 45 | 5 | 90 | 85 | 55 | 2 | 0 |
| | **Average (rounded)** | **48** | **68** | **74** | **74** | **74** | **42** | **45** |

---

## Table 2 — Commentary for Every Score

Each cell explains the score and highlights what is distinctive about that agent's implementation (or why the score is low). Specific files/dirs are cited as evidence.

| # | Feature | aider | claude-code | hermes | kilocode | openclaw | opencode | pi |
|---|---------|------|------|------|------|------|------|------|
| 1 | Multi-provider LLM support | LiteLLM (`llm.py`,`models.py`) + `resources/model-settings.yml`; OpenAI/Anthropic/Gemini/Bedrock/OpenRouter (`openrouter.py` scrapes model DB)/xAI/Deepseek/Vertex/Azure/Cohere; aliases + OAuth onboarding. | Anthropic-only: `client.ts`/`providers.ts` cover firstParty, Bedrock, Vertex, Azure Foundry — all serve Claude models; no OpenAI/Gemini/OpenRouter. | 28 provider profiles under `plugins/model-providers/` + declarative `ProviderProfile` ABC (`providers/base.py`); native transports + per-provider quirk hooks. | `provider/provider.ts` lazy-loads ~25 AI-SDK providers + Kilo gateway; `models-snapshot.ts` catalog backs "500+ models." | Built-in core providers (`register-builtins.ts`) + ~50 provider extensions; plugin-based provider registry rather than hardcoded clients. | `internal/llm/provider`+`models`: Anthropic/OpenAI/Gemini/Bedrock/Azure/Vertex/Copilot/OpenRouter/Groq/xAI+local; first-class Copilot OAuth token discovery. | `packages/ai/src/providers` first-class set + `models.generated.ts` ~30 namespaces; clean `Api`-keyed registry, lazy modules, per-provider OAuth. |
| 2 | Local / self-hosted models | Ollama (`models.py is_ollama`,`num_ctx`) + any LiteLLM endpoint via `--openai-api-base`; LM Studio/vLLM via OpenAI-compat path. | None: only `ANTHROPIC_BASE_URL` custom endpoint, which must speak the Anthropic API. | `ollama-cloud`+`lmstudio` providers (`lmstudio_reasoning.py`) + config aliases ollama/vllm/llamacpp; local STT/TTS too. | `@ai-sdk/openai-compatible` + Ollama embeddings; broad OpenAI-compat covers LM Studio/vLLM but no first-class local configs. | `extensions/{ollama,vllm,lmstudio,sglang,litellm}` + `node-llama-cpp` for local embeddings; no in-process chat inference. | `models/local.go` auto-discovers from `LOCAL_ENDPOINT` (LM Studio/OpenAI-style); endpoint env-only, Ollama not special-cased. | No dedicated Ollama class; `openai-completions` targets local servers, `overflow.ts` handles llama.cpp/LM Studio/Ollama overflow. |
| 3 | Model routing / per-role models | `main_model`+`weak_model` (commits/summaries)+`editor_model` w/ own edit format; architect routes planner→editor; `/model`,`/editor-model`,`/weak-model`. | `getSmallFastModel` (Haiku) for summaries/token-count/web-refine; `opusplan`/`sonnetplan` swap model in plan mode; hooks specify own model. | `auxiliary_client.py` pins per-task provider/model (vision, compression, title, embedding, session_search); `image_routing.py`. | Per-agent `model` field, global `small_model`, TUI switching, explicit plan→code "code-switch". | `model-selection-resolve.ts` fallback resolution + cross-provider failover + per-agent scope; fallback-centric not planner/editor split. | Distinct coder/task/title/summarizer agents each with own model; runtime switching via dialog; no planner-vs-editor split. | `model-resolver.ts` "scoped models" for runtime cycling (Ctrl+P) + per-call model/thinking overrides; no fixed roles. |
| 4 | Prompt caching | Anthropic beta + `cache_control` per-model + distinctive background cache-warming thread (`warm_cache`); cache-hit token accounting. | Best-in-class: `cache_control: ephemeral` 5m/1h TTLs, `promptCacheBreakDetection.ts`, `/break-cache`, cache-break analytics. | `prompt_caching.py` `system_and_3` 4-breakpoint layout w/ 5m/1h TTL targeting ~75% input savings. | Multi-provider (`transform.ts`): anthropic ephemeral, bedrock cachePoint, openai-compat, copilot; cache-read/write accounting. | Deep: `openai-prompt-cache.ts`,`google-prompt-cache.ts`, anthropic `cache-ttl.ts`, `prompt-cache-retention.ts`+`-observability.ts`; deterministic ordering mandated. | Anthropic-only (`anthropic.go`): ephemeral on system/last-tool/trailing msgs, sliding window; cost-tracked. OpenAI/Gemini lack markers. | Provider-specific: anthropic `cache_control` w/ TTL, `openai-prompt-cache.ts` key clamp; explicit cache-write/read cost accounting. |
| 5 | Reasoning / extended-thinking | `reasoning_effort`/`thinking_tokens` args+commands, `reasoning_tags.py` strips think tags, per-model `accepts_settings` gating. | `thinkingConfig` through `query.ts`, signature replay, redacted blocks, interleaved thinking, `/effort`,`/thinkback`. | `reasoning_config`/`thinking_budget` plumbed widely, `think_scrubber.py`, per-provider reasoning placement. | Per-provider in `transform.ts` (anthropic budgetTokens, deepseek/openai reasoning_content round-trip); catalog `reasoning:true`. | `embedded-agent-runner/thinking.ts` handles anthropic signatures+recovery, `--thinking medium`, reasoning-effort across providers. | Anthropic extended-thinking (`shouldThink` heuristic+temp bump) + OpenAI ReasoningEffort low/med/high; `CanReason` flags. | `anthropic.ts` adaptive thinking off→xhigh + interleaved beta; `models.ts` 6-level ladder, unified `ModelThinkingLevel` per-provider. |
| 6 | Streaming responses | On by default w/ live markdown renderer (`mdstream.py`,`run_stream`); clean non-stream fallback. | Full SSE (`Stream<BetaRawMessageStreamEvent>`) w/ non-streaming fallback + native-memory cleanup. | Throughout `conversation_loop.py` (delta scrubbers, mid-stream error recovery) + `stream_diag.py`; ACP/TUI consume chunks. | Fully streaming loop (`processor.ts`) consuming AI-SDK events, surfaced over SSE in `server/`. | `AssistantMessageEventStream` everywhere; all providers return AsyncIterable w/ channel-aware delivery policy. | `StreamResponse` event channel (`provider.go`), granular deltas accumulated per provider + rate-limit retry. | Core primitive: `AssistantMessageEventStream` typed events, all providers stream, fine-grained tool-streaming beta. |
| 7 | Search/replace (diff) editing | Flagship: `editblock_coder.py` robust block parsing, fenced + function-call variants; own SEARCH/REPLACE leaderboard. | `FileEditTool` old/new_string blocks + `replace_all`, quote/whitespace normalization (`utils.ts`); not multi-hunk. | `patch_tool(mode=replace)` old/new_string+replace_all (`file_tools.py`) w/ failure-hint escalation. | `tool/edit.ts` SEARCH/REPLACE w/ replacer chain (Simple/LineTrimmed/BlockAnchor) + structured diffs. | `edit.ts` `{oldText,newText}` arrays, multi-edit-per-call, non-overlap+uniqueness; handles JSON-stringified edits. | `tools/edit.go` old/new_string w/ uniqueness, read-before-edit, mod-time staleness; exact-match only. | `edit.ts`+`edit-diff.ts` `edits[]` arrays w/ uniqueness; batched multi-edit + per-edit fuzzy fallback. |
| 8 | Whole-file editing | `wholefile_coder.py` + func-call variants + `editor_whole_coder.py`; well-developed. | `FileWriteTool` create/overwrite w/ mod-time conflict detection + line-ending preservation. | `write_file_tool` w/ staleness/dedup tracking; escalation suggests full-file rewrite when patching fails. | `tool/write.ts` whole-file write computing two-file patch + BOM handling. | `createWriteTool`/sandboxed/host variants + apply_patch Add-File; param-validation + workspace-root guards. | `tools/write.go` create/overwrite w/ permission prompts + history versioning. | `tool/write.ts` whole-file write w/ same file-mutation-queue serialization as edit. |
| 9 | Unified-diff editing | `udiff_coder.py`+`udiff_simple.py` (pioneered udiff prompting) + OpenAI patch format (`patch_coder.py`). | No udiff edit tool; `diff` lib/`gitDiff.ts` only display diffs. | udiff generated for display/V4A (`_unified_diff`,`patch_parser.py`); not first-class input format (V4A is). | `apply_patch.ts`+`patch/index.ts` parse stripped envelope w/ `@@` hunks; add/update/delete/move. | `generateUnifiedPatch` is display-only; edits use Codex-style `apply_patch` hunks (`*** Update File`/`@@`). | `tools/patch.go`+`diff/patch.go` custom V4A envelope (not `diff -u` ingestion) but functionally close. | No udiff input; `generateUnifiedPatch` produces patches as output details only. |
| 10 | Multi-file coordinated edits | Edit blocks carry per-file paths; many files per turn + new-file creation. | Sequential FileEdit/FileWrite + sub-agents; per-path skill/LSP recompute; no atomic transaction. | V4A multi-file headers locked in sorted order via ExitStack (deadlock-safe), applied atomically. | `apply_patch` multi-section + todo-driven; loop-driven not transactional. | edit tool atomic multi-edit + apply_patch add/update/delete/move; `file-mutation-queue.ts` serializes writes. | `patch` tool atomic add/update/delete across files in one commit w/ per-file perms + combined diagnostics. | Implicit via loop; `file-mutation-queue.ts` serializes same-file, allows cross-file parallel; no atomic transaction. |
| 11 | Edit validation & auto-repair | Failed edits reflected back + auto-lint/auto-test feedback (`max_reflections=3`) + "did you mean" diagnostics. | `diagnosticTracker` + LSP `passiveFeedback.ts` republishes diagnostics post-edit; stale-file forced re-reads. | `agent/lsp/` feeds diagnostics into write/patch results; V4A `_validate_operations`; consecutive-failure hints. | Post-edit LSP diagnostics filtered to touched files fed back (`diagnostics.ts`); replacer chain auto-recovers. | LSP diagnostics fed back + structured `EDIT_MISMATCH_HINT`; rich failure classifier; no auto-retry loop. | Post-edit LSP diagnostics appended (`waitForLspDiagnostics`); model retries; no auto-rollback. | Structured `EditDiffError` fed back + `validateEditInput`; no lint loop/auto-retry, relies on model. |
| 12 | Fuzzy / flexible match application | Multi-strategy `search_replace.py flexible_search_and_replace` + `replace_most_similar_chunk` + SequenceMatcher 0.8. | `normalizeQuotes`+whitespace+markdown desanitize; `fuse.js` for file/cmd fuzzy; no Levenshtein block apply. | `fuzzy_match.py` chains exact/line-trim/whitespace/indent/unicode + re-indentation + escape-drift detection. | Rich: levenshtein scoring + Whitespace/Indentation/Escape/Trimmed/Context/MultiOccurrence replacers. | `edit-diff.ts normalizeForFuzzyMatch` (NFKC, smart-quotes/dashes/spaces, trailing-ws); Unicode-robust. | Patch parser fuzzy (exact→trimRight→trimSpace, fuzz>3 rejected); main edit tool exact-only. | `fuzzyFindText`+`normalizeForFuzzyMatch` exact-first then NFKC; round-trips original line endings. |
| 13 | Repo map / codebase indexing | Signature feature: `repomap.py` PageRank over symbol-ref graph (networkx), token-budgeted, tag caching, ContextCoder. | No persistent map; native-TS nucleo file index (`native-ts/file-index/`) + @-mention + CLAUDE.md injection. | No ranked repo-map; `skills/index-cache/` + `search_files` + LSP workspace symbols for navigation. | `explore` subagent + `packages/kilo-indexing/` (orchestrator/vector-store/cache) + instruction-tree walk. | Pluggable `context-engine/` (windowed/projected, token-estimated) + AGENTS.md inject; no tree-sitter map. | None; only `ls` of cwd injected into prompt + rule files; on-demand grep/glob/agent search. | None; explicit reads + AGENTS.md/CLAUDE.md + skills; deliberately minimal, model uses grep/find. |
| 14 | Tree-sitter / AST parsing | `grep_ast`/`tree_sitter` + bundled `.scm` queries for ~30+ langs (`queries/`); `get_tags` extracts defs/refs. | Real tree-sitter (WASM) but scoped to **bash security parsing** (`bashPermissions.ts`), not code nav. | No tree-sitter; only Python `ast` for tool-registry/skill audits; structural understanding via LSP. | `kilo-indexing/src/tree-sitter/` (languageParser + per-lang queries) for semantic chunking. | None found; structural understanding delegated to LSP. | None; only LSP language-ID mapping (`lsp/language.go`); no in-process AST. | None anywhere (only unrelated "vector" hits + highlight.js). |
| 15 | Semantic / embedding search (RAG) | No code RAG; `/help` uses llama_index embeddings over **docs** only (`help.py`). | None; all search lexical (grep/glob/ripgrep) or fuzzy-string (nucleo/fuse). | None over codebase; "embedding" only aux-task config; no faiss/chroma/qdrant index. | Real: `lancedb.ts` (`@lancedb/lancedb`)+`kilo-indexing` embedders/search-service, multi-embedder. | `memory-lancedb` vector search (auto-recall/capture) + `voyage` embeddings; scoped to memory not codebase. | None; search purely lexical; db stores sessions not embeddings. | None of any kind in source. |
| 16 | Grep / regex / glob search | No agent-callable search tool; `/add` globbing + grep_ast underlies repomap. | Comprehensive: `GrepTool` (ripgrep)+`GlobTool`+native fuzzy `FileIndex`; core strength. | `search_tool` regex/glob content+filename w/ context lines + LSP symbol search. | `grep.ts` (ripgrep)+`glob.ts`+`warpgrep.ts`; bundles ripgrep binary. | Grep/glob/read via sessions tool suite + exec; TUI fuzzy-filter; no bespoke rg module surfaced. | `grep.go` (ripgrep+regex fallback)+`glob.go` (doublestar)+`ls`; plus Sourcegraph cross-repo search. | `grep.ts` (ripgrep auto-downloaded)+`find.ts` (fd)+`ls.ts` gitignore-aware; auto-provisions binaries. |
| 17 | LSP integration | None; only a stray `pyrightconfig.json` filename reference. | Full: `services/lsp/` + `LSPTool` (hover, symbols, go-to-def, call-hierarchy); diagnostics fed post-edit. | `agent/lsp/` ~4,300 LOC (client/manager/protocol/auto-install) supporting pyright/ts/gopls/rust/clangd. | Full: `lsp/` + `tool/lsp.ts` (definition, references, hover, symbols, implementation, call-hierarchy). | Hand-rolled JSON-RPC client (`agent-bundle-lsp-runtime.ts`) hover/def/refs/diagnostics, per-bundle config. | Comprehensive hand-rolled client (`internal/lsp/`) + workspace watcher; only diagnostics exposed to model. | None; "diagnostics" files are internal runtime, not LSP. |
| 18 | Shell / command execution | `run_cmd.py` (pexpect/subprocess) via `/run`,`/git`, LLM-suggested cmds; always user-confirmed. | Best-in-class: `BashTool`+`PowerShellTool`, persistent sessions, sandbox hooks, classifiers, tree-sitter parsing, bg tasks. | `terminal_tool.py`+`process_registry.py` managed long-running procs; approval/sandbox/checkpoint integrated. | `tool/shell.ts`+PTY layer, bash-hierarchy tracking, background processes, permission-gated. | `bash-tools` + exec-approval policy + safe-bin profiles + sandboxed exec (Docker/SSH); very comprehensive. | `bash.go` persistent shell session, timeouts, banned-cmd list, safe-read-only allowlist. | `bash.ts`+`bash-executor.ts`+`exec.ts` w/ bounded output, idle-timeout, remote/SSH delegation, child cleanup. |
| 19 | Tool / function-calling framework | Limited: some coders use OpenAI functions; loop handles tool_calls but no general registry. | Mature: `Tool.ts buildTool`/`ToolDef` + `tools.ts` registry (lazy gating) + `ToolSearchTool` + deny-rule merging. | `registry.py` auto-discovery + toolset aliases + `toolsets.py`/distributions + tool_search; ~82 tool modules. | `tool/tool.ts Tool.define` + central `registry.ts` (built-ins+MCP+plugin+Kilo) + per-model/agent filtering. | TypeBox-schema tools + adapter pipeline + before/after hooks + policy filtering + tool-search/split. | `BaseTool` interface (`tools.go`) per-agent registry; MCP+sub-agent wrapped as same type. | `packages/agent/src` standalone framework: `AgentTool` types + `validateToolArguments` (typebox) + registry. |
| 20 | MCP client | None (no "mcp" hits in source). | Full: `services/mcp/` stdio/SSE/HTTP/SDK, OAuth, elicitation, registry, per-channel perms. | `mcp_tool.py` ~3,800 LOC: stdio/HTTP/SSE, OAuth, env filtering, resources; `optional-mcps/` bundled. | `mcp/index.ts` Stdio/SSE/HTTP + OAuth, tools/prompts/resources, `kilo mcp` CLI. | Official `@modelcontextprotocol/sdk` (`agent-bundle-mcp-runtime.ts`) stdio/sse/http + per-bundle config. | stdio+SSE via `mark3labs/mcp-go` (`mcp-tools.go`); no resources/prompts, new client per call. | None; explicitly "No MCP" by design (`README:474`). |
| 21 | MCP server | None. | Multiple: standalone source-exploration MCP server (`mcp-server/`) + in-process Chrome MCP + VSCode SDK. | `mcp_serve.py` 10-tool stdio server + re-exposes Hermes tools to Codex via MCP; dual role. | No self-as-MCP; VS Code ext registers external Playwright MCP, but agent not exposed; uses ACP instead. | `mcp/tools-stdio-server.ts`+`channel-server.ts`+`plugin-tools-serve.ts` serve OpenClaw tools/channels over MCP. | None; MCP-client-only. | None; same intentional omission. |
| 22 | Web search | None (no Tavily/Bing/SERP). | Anthropic server-side `web_search` w/ domain filtering + Haiku refinement. | `web_tools.py`+plugins: Exa/Tavily/Parallel/Firecrawl/SearXNG/Brave/DDGS/xAI w/ auto-backend selection. | `websearch.ts` (Exa livecrawl/deep) + `mcp-exa.ts`; permission-gated. | Provider plugins brave/exa/tavily/ddg/searxng/perplexity/firecrawl + `web-search.ts` citation handling. | None (Sourcegraph is code search, not web). | None (only a name-mapping table entry). |
| 23 | Web / URL fetch & scrape | `/web`+`scrape.py` (Playwright/httpx)→markdown; auto-detect URLs in chat. | `WebFetchTool`→markdown + prompt extraction + preapproved hosts + SSRF guard. | `web_extract` (Firecrawl/Tavily/Exa)→markdown w/ `website_policy.py`/`url_safety.py`. | `webfetch.ts`→markdown via Turndown + image fetch + format negotiation. | `web-fetch.ts` readability extraction + HTML→md/text + SSRF guard + Cloudflare fallback. | `fetch.go`→text/md/html (html-to-markdown+goquery), 5MB cap, permission-gated; single-URL. | None implemented (only OAuth browser-open + name-mapping entry). |
| 24 | Browser automation | Headless Playwright fetches single page HTML only (`scrape.py`); no browser driving. | "Claude in Chrome" native-host+MCP bridge w/ permission modes; needs external extension. | Deep: `browser_tool.py` ~3,800 LOC + CDP + Camoufox anti-bot + supervisor + full `computer_use/` package. | Only in VS Code ext (Playwright MCP); CLI core has no native driver. | `extensions/browser` drives Chrome via CDP (click/type/nav/screenshot) + profiles + noVNC + sandbox. | None. | None (no puppeteer/playwright). |
| 25 | Image / multimodal input | `get_images_message` image_url payloads gated by `supports_vision`; handles images + PDFs. | `FileReadTool` reads images (base64, resized) + PDFs; clipboard-image paste hints. | `vision_tools.py`+`image_routing.py` auto-attaches natively or routes to aux vision model. | `read.ts` returns images as data URLs (jpeg/png/gif/webp); message-v2 image parts. | `run/images.ts` broad format detection + sanitization + `minimax-vlm` + PDF native pipeline. | `attachment.go`+BinaryContent; TUI filepicker (≤5 imgs), encoded per provider; gated by `SupportsAttachments`. | `read.ts` MIME detection + auto-resize 2000px gated by model input; clipboard paste + EXIF. |
| 26 | Image / media generation | None. | None (no DALL-E/Imagen/diffusion). | `image_generation_tool.py` (fal) + `video_generation_tool.py` + image_gen/video_gen plugins. | None found. | image/video/music gen (`src/{image,video,music}-generation/`) + fal/comfy/runway/pixverse plugins. | None. | `ai/src/images.ts`+`images-api-registry` + OpenRouter images provider; single-provider, not an agent tool. |
| 27 | Autonomous agentic loop | Bounded reflection loop (`max_reflections=3`) re-prompting on edit/lint/test errors, not general tool loop. | Full reason-act loop in `query.ts`/`QueryEngine.ts` (68k+ LOC) w/ streaming, tools, compaction, stop hooks. | `conversation_loop.py` ~4,700 LOC + `iteration_budget.py` (90 iters, 50 subagent) + interrupts. | Robust loop `session/processor.ts` (streaming steps, tool dispatch, compaction/continue/stop, retry). | `agent-core/agent-loop.ts` + large `run/` attempt pipeline (bootstrap→prompt→tools→compaction→failover). | `agent.go processGeneration` loops while ToolUse w/ cancellation, panic recovery, persistence. | `agent-loop.ts` reason→act (sequential/parallel batches) + mid-run steering injection. |
| 28 | Planning / plan mode | Architect/plan mode (`architect_coder.py`+`ask_coder.py`) separates planning from execution. | Rich: `EnterPlanModeTool`/`ExitPlanModeV2Tool` + plan sub-agent + interview-phase + `/plan`,`/ultraplan`. | Plan exists as a skill (write markdown plan, no exec) + writing-plans/spike skills; not a hard mode. | First-class: built-in `plan` agent (read-only) + `plan_enter`/`plan_exit` tools + reminders. | `update_plan` tool (ordered steps, one in_progress) but skill/persona-driven, not a locked read-only mode. | None; generic agent prompt, no plan/act toggle. | None; intentionally omitted (`README:480`), delegated to extensions. |
| 29 | Sub-agents / task delegation | No true sub-agents; architect mode internally creates a child editor coder only. | Extensive: `AgentTool` spawns child agents (general/Explore/Plan/verification) + multi-agent fan-out + Teams. | `delegate_tool.py` spawns child AIAgents + parallel swarm (ThreadPool) + per-subagent approval; no recursion. | `tool/task.ts` spawns child sessions (general/explore/custom); restricted nesting, inherited perms, cost propagation. | `sessions_spawn`/`subagents`/`sessions_yield` + ACP-spawn + controller/registry/liveness + A2A delivery. | `agent` tool spawns stateless read-only `task` child in own session, cost rolls up; single-level, no fan-out. | None; explicitly omitted (`README:476`), suggested via tmux/extensions. |
| 30 | Todo / task-list tracking | None (only `# TODO` comments). | `TodoWriteTool` + watchers + separate Tasks v2 system (Create/Get/List/Update/Stop). | `todo_tool.py` per-session TodoStore w/ status validation, dedupe, post-compression re-injection. | `tool/todo.ts` (TodoWrite) + `session/todo.ts` + view rendering (pending/in_progress/completed/cancelled). | `update_plan` step list + goal tools + durable task registry (`src/tasks/`); folded into plan tool. | None. | None; intentionally omitted ("they confuse models" `README:482`). |
| 31 | Custom agent modes / personas | Built-in chat modes (`/code`,`/ask`,`/architect`,`/context`,`/help`) + `--chat-mode`; fixed not user-defined. | Output styles (`outputStyles/`) + custom agents from `.claude/agents` + "buddy" companion persona. | "Souls" (SOUL.md) + profiles (`profiles.py`) + toolset distributions + gateway personas. | Built-in build/plan/general/explore + user agents from `.kilo/agent/*.md` w/ per-mode perms/models. | Per-agent scope/config (`agents.config.ts`) + named agents w/ own models/auth + persona skills + channel binding. | Four built-in personas (coder/task/title/summarizer) w/ provider-specific prompts; fixed, no user modes. | `--system-prompt`/`--append-system-prompt` + extensions rewrite prompt/register modes; no persona manager. |
| 32 | Scheduled / background / cron tasks | None (only cache-warming/watch threads). | `ScheduleCronTool` (Create/Delete/List) + `/schedule`,`/loop` + remote agents + background tasks. | Full cron (`cron/scheduler.py`,`cronjob_tools.py`) file-locked + background review + gateway async. | Background processes + remote relay + claw cloud sessions; no cron/scheduled-task system. | `src/cron/` at/every/cron + isolated-agent execution + delivery dispatch + gateway cron protocol. | None (summarizer runs inline). | None (setInterval is UI/git-watcher only; bg bash excluded). |
| 33 | Context compaction / token mgmt | `ChatSummary` (`history.py`) recursively summarizes old msgs w/ weak model; single strategy. | Sophisticated `services/compact/`: auto-compact thresholds, micro-compact, session-memory compact, warnings. | `trajectory_compressor.py` ~1,500 + `context_compressor.py` ~2,000 + `context_engine.py`; per-model thresholds. | `compaction.ts` (anchored summaries, chunked recovery) + overflow + summary + plugin hooks. | Extensive `compact*.ts`: overflow/manual/timeout-triggered + loop guards + tool-result truncation + budgeting. | `summarizer` agent summarizes whole convo, marks `SummaryMessageID`, slices history; auto-compact at 95%. | `compaction.ts` (cut-point selection, file-op tracking across compactions) + branch-summarization. |
| 34 | Conversation persistence & history | Input/markdown/LLM-request history files (`io.py`); `/save`,`/load` file context. | Per-session `.jsonl` transcripts (`sessionStorage.ts`) project-scoped + transcript search. | `hermes_state.py` SQLite/WAL state.db storing history/config + `session_search_tool.py`. | SQLite (Drizzle) `storage/` + JSON migration; full message-v2 event history. | Transcript store (`transcripts/store.ts` JSONL) + rewrite/sanitization + session lifecycle events. | SQLite (`internal/db/` sqlc) sessions/messages/tool-calls/file-history; debug JSON dumps. | `session-manager.ts` append-only JSONL **trees** (id/parentId, versioned migrations) + jsonl-repo mirror. |
| 35 | Session resume / restore | `--restore-chat-history` replays markdown + `/load`; no rich resumable objects. | Full: `/resume` (cross-project) + `--continue` + teleport resume + agent resume + snapshot restore. | `--resume <id>` + recap tuning (`session_recap.py`) + one-shot `/resume`. | `kilo run --continue/--session/--fork` + cloud-fetch continue + session import + revert/unrevert. | `--session-key`/`--session-id` + TUI last-session restore + session-manager cache. | Session dialog (`dialog/session.go`) backed by session.List; TUI-driven, no `--continue` flag. | `--continue`/`--resume`/`--session`/`--session-id` + session-picker + `/resume`+`/tree`. |
| 36 | Cost / token usage tracking | Per-msg/session cost + cache-token math (DeepSeek/Anthropic) + `/tokens`,`/cost` + LiteLLM fallback. | Best-in-class `cost-tracker.ts`+`costHook.ts`: tokens, cache splits, lines changed, API duration + `/cost`,`/usage`,`/stats`. | `usage_pricing.py` ~900 + `account_usage.py` w/ models.dev pricing; runtime footer + reporting. | `session.ts` input/output/reasoning/cache + provider-reported cost + `kilo stats` + subagent propagation. | `usage-accumulator.ts`/`usage-reporting.ts` (in/out/cacheRead/Write+cost) + model-catalog pricing + skill. | Per-model cost from rates incl cache (`TrackUsage`) shown in sidebar; child cost rolls up. | `models.ts calculateCost` (in/out/cache breakdown) + per-msg usage + `/session` cost; no always-on footer. |
| 37 | Long-term / cross-session memory | None; conventions/read-only files are manual context, not learned memory. | Cross-session: `memdir/` auto-memory (relevance/aging) + team memory sync + `SessionMemory` + remember skill. | `memory_tool.py`+`memory_manager.py` persistent store + `background_review.py` forks agent to persist memories. | `recall.ts` (`kilo_local_recall`) searches past session transcripts across worktrees; semantic not facts. | `memory-{core,lancedb,wiki,active}` auto-recall/capture + `memory-host-sdk` + root MEMORY.md; multi-backend. | Limited to `OpenCode.md` convention + per-session file history; no auto long-term store. | None dedicated; only static AGENTS.md/CLAUDE.md + skills; docs suggest TODO.md via extensions. |
| 38 | Git auto-commit / integration | Core: `repo.py GitRepo` auto-commits each AI edit w/ LLM message + authorship attribution flags. | Deep: `/commit`,`/commit-push-pr`, git utils, git-bundle teleport, snapshots, GitHub App; user-initiated commits. | Mainly shadow-git checkpoint store (`checkpoint_manager.py`); auto-commit not default, PR/commit via skills. | `git.ts` + AI commit-message gen + Agent Manager GitOps/pollers; assisted not fully auto. | No auto-commit; `git-commit.ts` only reads build hash; git via shell + github skills. | No auto-commit; opinionated git workflow embedded in bash-tool prompt; git detected for context. | No auto-commit in core; `git.ts` only parses URLs; commits when model runs git via bash (`/wr`). |
| 39 | Undo / change review / diff | `/undo` reverts only aider's commits (safety checks) + `/diff` + colorized diffs. | File-history snapshots per message + `/rewind` selector + turn diffs + IDE diff. | `checkpoint_manager.py` auto-snapshots before mutations + rollback + `_unified_diff`. | Shadow-git snapshots (separate git-dir) + revert/unrevert + `buildFileDiff` + per-tool diff in TUI/webview. | TUI diff rendering + unified-patch per edit; permission gating substitutes for undo; no checkpoint/revert. | Syntax-highlighted unified+side-by-side diffs (`diff/diff.go`) in permission dialogs + file-version history. | Colored unified diffs (`diff.ts`) + edit patches + session fork/clone/tree; undo via fork-to-earlier. |
| 40 | PR / code-review automation | None; only a GitHub-issue prefill URL for crash reports. | `/commit-push-pr`,`/pr_comments`,`/review`,`/autofix-pr` + GitHub App/Actions + `/security-review`. | Skill-driven: github-pr-workflow/code-review/requesting-review skills + gh flows; no built-in PR tool. | `/review`,`/local-review` commands + `cli/cmd/pr.ts` checkout + PR status poller; creation via gh/agent. | Skill-driven: github/gh-issues skills + ClawSweeper review policy + github plugins; via gh CLI. | No automation; only prompted `gh pr create` workflow in bash description. | No PR tool; bundled prompts (`.pi/prompts/pr.md`,`wr.md`) + `/share` gist; prompt-driven via gh. |
| 41 | CLI / headless mode | `--message`/`--message-file` one-turn + `--yes-always` + scriptable Python API + JSON/quiet. | Robust `cli/print.ts` (`-p`/print) + structured stream-json I/O + non-interactive handling. | Oneshot `-z` + `--query` + `batch_runner.py` parallel + `mini_swe_runner.py` + TTY-aware fallbacks. | Headless `kilo run` (`--continue`) + `kilo serve` (HTTP); full subcommand suite. | `agent --message --json` + `register.message.ts` + json-mode + standalone `openclaw.mjs` + `--deliver`. | `-p/--prompt` + `-f text/json` + `-q` quiet + auto-approve; JSON is final-content only. | `--print/-p` + `--mode text/json/rpc` + `--list-models` + @-args; JSON event-stream + RPC mode. |
| 42 | Interactive TUI | `prompt_toolkit`+`rich`: multiline, autocomplete, live markdown, spinners, file-completion (io.py 43KB). | Best-in-class: custom React/Ink reconciler (yoga-layout, bidi, hit-testing, virtual scroll) + vim + overlays. | `cli.py` ~706KB (prompt_toolkit) overlays/spinners/keybinds + separate React/xterm ui-tui + tui_gateway. | SolidJS TUI on `@opentui/core`+`@opentui/solid` (components, dialogs, autocomplete, process list). | `@earendil-works/pi-tui`: chat-log, markdown, diff view, fuzzy select, OSC8 links, themes, overlays. | Polished Bubble Tea: streaming markdown, sidebar, many dialogs, 9 themes, image support. | Bespoke `packages/tui` (differential renderer, editor w/ kill-ring/undo, markdown, terminal-image, fuzzy). |
| 43 | IDE / editor extension | No bundled extension; only watch-mode AI comments + `--editor`; 3rd-party plugins external. | Deep IDE client (`utils/ide.ts`) VSCode+JetBrains via lockfile/MCP + at-mention/selection sync/diff. | ACP (`acp_adapter/`,`acp_registry/`) lets Zed drive Hermes + copilot-acp; no native VSCode/JetBrains. | Three: VS Code ext (`kilo-vscode/`) + JetBrains plugin + Zed via ACP. | ACP (`src/acp/`+`acpx`) enables editors like Zed; ACP-based not bundled extension. | None. | None (vscode strings only Copilot OAuth). |
| 44 | Desktop GUI app | None (browser GUI is a web server). | No native shell; `/desktop`,`/mobile` commands + web app but no Electron/Tauri. | None (no Electron/Tauri/Qt). | None; VS Code Agent Manager is an extension panel not standalone. | Native apps: `apps/macos` (SwiftUI) + `apps/ios` + `apps/android` + macos-mlx-tts. | None. | None. |
| 45 | Web UI / server / API | Streamlit GUI (`gui.py`, `--gui`): chat/files/diffs/undo; single-user local, no API. | Next.js app (`web/`) + PTY-backed web terminal server (`server/web/` WebSocket) + remote bridge. | `web/` React/Vite SPA + `gateway/api_server.py` + many messaging adapters + tui_gateway websocket. | `kilo serve` (HTTP API) + `kilo web` (mDNS, password) + `server/` HTTP+SSE + auto-gen SDK. | `ui/` Lit/Vite control SPA served by gateway HTTP/WS + gateway-protocol package + client. | None (TUI+CLI only). | None; RPC mode is stdin/stdout JSON not a web server. |
| 46 | Voice input / speech / TTS | `voice.py` records (sounddevice) + Whisper transcription via `/voice`; input-only, no TTS. | Comprehensive: native audio capture (cpal) + WebSocket STT + keyterm biasing + push-to-talk. | STT (faster-whisper/Groq/OpenAI) + TTS (Edge/ElevenLabs/OpenAI/NeuTTS) + `voice_mode.py` live. | None (only TUI notification bell). | Exceptional `speech-core` + `talk/` (voice-call/realtime) + `tts/` + many speech plugins; two-way. | None. | None (audio refs only model-pricing fields). |
| 47 | Custom slash commands | ~45 `cmd_*` methods (`commands.py`) w/ autocomplete + `/load` scripts; not user-extensible w/o code. | ~90 built-ins + markdown custom commands from `.claude/commands` (frontmatter, args, !-exec). | `commands.py` COMMAND_REGISTRY feeding CLI/gateway/Telegram/Slack + per-platform access; skills as commands. | Markdown templates (`$ARGUMENTS`/`$1`) from `.kilo/command/*.md` + built-ins + MCP-prompt commands. | Chat-command discovery (`chat-commands.ts`) + CLI registry + plugin-manifest aliases. | Markdown `.md` commands from config dirs w/ `$NAMED` placeholders + interactive args + `/init`,`/compact`. | Built-in slash (`slash-commands.ts` ~22) + prompt templates (`.pi/prompts/*.md`) + extension commands. |
| 48 | Hooks / lifecycle events | None; only `--test-cmd`/`--lint-cmd` post-edit hooks. | Best-in-class: 13+ events (PreToolUse, PostToolUse, SessionStart/End, PreCompact...) w/ prompt/agent/HTTP executors + SSRF. | Two systems: agent shell_hooks (pre/post tool w/ consent) + gateway event hooks (startup/session/agent/command). | Plugin Hooks: tool.execute.before/after, chat.message/params, permission.ask, command.before + experimental transforms. | `src/hooks/` (frontmatter hooks, install/bundled, fire-and-forget) + before/after-tool + compaction hooks. | None general; only internal pubsub. | Exceptional: ~40 typed events (agent/turn/message/tool/before_provider_request/session/compact) w/ mutable results. |
| 49 | Plugin / extension ecosystem | None; extensibility via config/conventions only. | Full: built-in plugins + marketplace install + plugin-provided skills/hooks/MCP + reload. | `plugins/` ~20 categories user-overridable + `hermes plugins enable`; provider profiles are plugins. | `packages/plugin/` interface + loader (remote/local) + built-in auth plugins + `kilo plug` CLI. | Massive: 131 manifests across `extensions/` + contract-driven loader + plugin-sdk; standout. | None beyond MCP + markdown commands; no plugin API. | Full `package-manager.ts` installs extensions/skills/prompts/themes from npm+git w/ supply-chain hardening. |
| 50 | Skills system | None. | Best-in-class `skills/`: bundled + `.claude/skills` + conditional/path-activated + SkillTool + `/skillify`. | `skills/`+`optional-skills/` + `skills_hub.py` ~3,700 + manager/sync/provenance/audit + background curator. | `skill/index.ts`+discovery loads SKILL.md from `.kilo/`,`.claude/`,`.agents/` + registry download + builtin. | `skills/` 50+ + `src/skills/` (discovery/loading/precedence/sandbox-sync) + skill-workshop capture. | None. | `skills.ts` frontmatter skill packages, model-invocable via `<skill>` blocks, via package manager. |
| 51 | Project rules / context files | `/read`,`--read` of CONVENTIONS.md + read-only files + `.aider.conf.yml`; no auto AGENTS.md discovery. | CLAUDE.md (project/user/enterprise) + @-imports + AGENTS.md + output-styles + auto/team memory. | `prompt_builder.py` discovers AGENTS.md/.hermes.md/SOUL.md/.cursorrules w/ injection scanning. | `instruction.ts` discovers AGENTS.md+CLAUDE.md (project+global) + `kilo.json` + URL instructions + soul.txt. | AGENTS.md canonical (`workspace.ts`) + CLAUDE.md symlinks + root MEMORY.md + migrate-claude. | `contextPaths`: CLAUDE.md/.cursorrules/.cursor/rules/copilot-instructions/OpenCode.md concurrently loaded. | `resource-loader.ts` loads AGENTS.md/CLAUDE.md as `<project_instructions>` + `--append-system-prompt` files. |
| 52 | SDK / programmatic API | `Coder.create(...)` documented but "not officially supported"; usable as library not stable SDK. | Mature `entrypoints/sdk/` (Zod schemas, generated types) + control transport + stream-json; CLI as backend. | `run_agent.py AIAgent.run_conversation` reused by batch/ACP/gateway/MCP/subagents; no formal package. | Auto-gen `@kilocode/sdk` over server API + ACP SDK; products embed CLI. | `@openclaw/sdk` + `agent-core` (reusable agentLoop) + plugin-sdk barrels; sdk private/0.0.0. | No published SDK; service-oriented internals + config schema generator; internal-only. | Strong `core/sdk.ts createAgentSession` + exports + RPC + separate publishable pi-agent-core/pi-ai. |
| 53 | Permission / approval system | `confirm_ask` (yes/no/never/all) + ConfirmGroup; shell+untracked-edit confirm; `--yes-always` override. | Best-in-class: permission modes + bash/yolo classifiers + dangerous-pattern + rule parser + AskUserQuestion. | `approval.py` ~1,600 hardline blocklists + sudo guards + aux-LLM smart auto-approve + `--yolo`. | Fine-grained `permission/` per-tool/pattern allow/ask/deny + bash allowlists + plugin hook + ACP guards. | exec-approvals (off/on-miss/always + allowlist + cmd analysis) + ACP classifier + policy pipeline. | Mature `permission.go`: per-tool approval w/ diff + one-time/session grant + read-only allowlist. | Minimal: explicitly "No permission popups"; only UI confirm() + tool allowlist; approval via extensions. |
| 54 | Sandboxing / isolation | None; only Ruff `--isolated` flag (unrelated); Docker images but no in-process isolation. | Strong `@anthropic-ai/sandbox-runtime` (seatbelt/bubblewrap FS deny + network policy) + sandbox doctor. | Multiple environments (`tools/environments/`): local/docker/modal/daytona/singularity/ssh + path_security. | Git-worktree isolation + cloud claw sandboxes + permission gating; no OS-level sandbox (no seccomp/bwrap). | Best-in-class: Docker+SSH backends + fs-bridge + `validate-sandbox-security.ts` + `@openclaw/net-policy`. | Limited: banned-cmd list blocks network tools + injection warning; no real sandbox/container/net policy. | None built-in; "sandbox" code only recovers env when pi runs inside external sandbox; docs suggest container. |
| 55 | Security review / scanning | None. | `/security-review` (senior-security prompt over diff) + SSRF guards + secret-guard + security skill; prompt-driven. | `tirith_security.py`+`threat_patterns.py`+`osv_check.py` (OSV) + url_safety + security-guidance plugin + audits. | Security review prompt (`local-review.txt` security/perf findings) via `/local-review`; no SAST/secret engine. | OpenGrep rulepack + dependency-vuln gate + GHSA scripts + `src/security/audit-*` suite + `/security-review`. | None; prompt merely permits analyzing vulnerabilities. | None in source (npm audit is CI tooling). |
| 56 | Test / lint runner integration | Strong: `--auto-lint`/`--auto-test` + built-in tree-sitter linter + Ruff; failures fed into reflection loop. | Verification sub-agent + `/init-verifiers` (Playwright/Tmux/HTTP) + verify skill + LSP diagnostics post-edit. | LSP diagnostics fed back + TDD/debugging skills + tests via terminal + mini_swe/batch harness; no always-on tool. | LSP diagnostics fed back post-edit + shell-run tests; no dedicated framework but real diagnostics loop. | Shell exec + skills (`pnpm test`/`check`) + LSP feedback + qa extensions; no structured runner tool. | No dedicated runner; tests run as ordinary bash; closest feedback is LSP diagnostics. | No integration; tests via bash only; `npm run check` is contributor cmd, model interprets results. |
| 57 | Telemetry / analytics | PostHog (opt-in prompt) + permanently_disable + custom host (`analytics.py`); privacy-conscious. | Heavy: OpenTelemetry metrics/logs/traces + BigQuery exporter + Datadog + GrowthBook/Statsig + tengu events + opt-out. | Langfuse plugin (traces conversations/LLM/tools) + skill-usage tracking; opt-in observability not default analytics. | `kilo-telemetry/` (PostHog+OpenTelemetry) wired through indexing/review/sessions; opt-out configurable. | diagnostics-otel (OpenTelemetry) + diagnostics-prometheus + `sessions/telemetry.ts`; opt-in observability. | None (only a prompt-text mention; nothing collected). | Minimal opt-in: `telemetry.ts` 13-line flag gate; no analytics pipeline. |
| 58 | Internationalization (i18n) | Output i18n: `--chat-language`/`--commit-language` + babel locale detection; UI strings not localized. | None; UI hardcoded English (intl/locale hits are Unicode width/bidi rendering). | Full: `locales/` 16 language YAMLs + `i18n.py` + web UI i18n + docs translation; localized agent+UI+docs. | Strong: `kilo-i18n/` 20 locales merged in VS Code webview; CLI itself mostly English. | Partial: wizard i18n (en/zh-CN/zh-TW) + control-UI i18n + extensive docs i18n; not full runtime translation. | None; English-only (locale refs only in generated LSP types). | None; hardcoded English (localeCompare only for sorting). |

---

## Feature Descriptions

Definitions used to score each feature (these explain what each row name means).

### A. Models & Providers
1. **Multi-provider LLM support** — Breadth of supported model vendors/APIs (OpenAI, Anthropic, Google, OpenRouter, Bedrock, Azure, etc.) and how cleanly providers are abstracted.
2. **Local / self-hosted models** — First-class support for running against local/offline inference servers (Ollama, llama.cpp, LM Studio, vLLM, SGLang) beyond a generic OpenAI-compatible base URL.
3. **Model routing / per-role models** — Ability to assign different models to different roles/tasks (e.g. a planner vs. an editor vs. a cheap summarizer model) and to switch models at runtime.
4. **Prompt caching** — Explicit use of provider prompt-cache mechanisms (cache_control / ephemeral markers, TTLs, cache-key stability) and accounting of cache-read/write tokens.
5. **Reasoning / extended-thinking** — Support for reasoning/thinking models (thinking budgets, reasoning-effort levels, think-tag handling, interleaved thinking, signature replay).
6. **Streaming responses** — Token/event streaming from the model to the UI with granular event types and fallbacks.

### B. Code Editing
7. **Search/replace (diff) editing** — A SEARCH/REPLACE or old_string→new_string block edit format for targeted changes.
8. **Whole-file editing** — Creating or fully overwriting a file's contents.
9. **Unified-diff editing** — Applying edits expressed as a unified/`@@`-hunk diff (or an equivalent structured patch envelope) as the *input* edit format.
10. **Multi-file coordinated edits** — Applying changes across multiple files within one operation/turn, ideally atomically.
11. **Edit validation & auto-repair** — Detecting failed/ambiguous edits or post-edit syntax/lint/type errors and feeding them back so the model self-corrects.
12. **Fuzzy / flexible match application** — Tolerant application of edits when the model's text doesn't match byte-for-byte (whitespace/indentation/Unicode normalization, similarity matching).

### C. Codebase Understanding
13. **Repo map / codebase indexing** — A precomputed map or ranked context-selection mechanism that summarizes the repository's structure/symbols for the model.
14. **Tree-sitter / AST parsing** — In-process syntactic/AST parsing of source code (for symbol extraction, chunking, navigation, or security analysis).
15. **Semantic / embedding search (RAG)** — A vector/embedding index enabling semantic retrieval over the codebase (or memory).
16. **Grep / regex / glob search** — Lexical search tools (ripgrep/grep, glob, fd) exposed to the agent.
17. **LSP integration** — A language-server client providing diagnostics, hover, go-to-definition, references, symbols.
18. **Shell / command execution** — Running shell/terminal commands, ideally with persistent sessions, timeouts, and background processes.

### D. Tools & Capabilities
19. **Tool / function-calling framework** — A general tool abstraction/registry (schemas, dispatch, hooks, filtering) rather than hardcoded behaviors.
20. **MCP client** — Consuming external Model Context Protocol servers (stdio/SSE/HTTP, OAuth, resources/prompts).
21. **MCP server** — Exposing the agent itself (or built-in servers) over MCP for other clients.
22. **Web search** — Querying a web search engine/provider.
23. **Web / URL fetch & scrape** — Fetching URLs and converting page content to text/markdown (with safety guards).
24. **Browser automation** — Driving a real/headless browser (CDP/Playwright) for clicking, typing, navigation, screenshots.
25. **Image / multimodal input** — Accepting images/PDFs as model input (vision), with format handling and model gating.
26. **Image / media generation** — Generating images/video/audio via generative models.

### E. Agent Loop & Orchestration
27. **Autonomous agentic loop** — A multi-step reason→act→observe loop that executes tools and iterates toward a goal.
28. **Planning / plan mode** — A dedicated planning/architect mode (often read-only) that produces a plan before execution.
29. **Sub-agents / task delegation** — Spawning child agents to handle sub-tasks (with isolation, restricted toolsets, cost roll-up, fan-out).
30. **Todo / task-list tracking** — A structured to-do/task-state mechanism the agent maintains across a session.
31. **Custom agent modes / personas** — User-definable agents/modes/personas with their own prompts, tools, or models.
32. **Scheduled / background / cron tasks** — Running agent work on a schedule or in the background asynchronously.

### F. Context & Session
33. **Context compaction / token management** — Summarizing/truncating conversation to stay within token budgets (auto-compact thresholds, overflow handling).
34. **Conversation persistence & history** — Durable storage of conversation/messages/tool calls (files or a database).
35. **Session resume / restore** — Resuming/continuing prior sessions (`--continue`/`--resume`, pickers, forking).
36. **Cost / token usage tracking** — Tracking and reporting token usage and dollar cost (including cached-token accounting).
37. **Long-term / cross-session memory** — Persistent memory that carries learned facts/context across sessions (beyond static rule files).

### G. Git & Review
38. **Git auto-commit / integration** — Built-in git operations, especially automatic commits of agent edits with generated messages and authorship handling.
39. **Undo / change review / diff** — Presenting diffs and reverting changes (checkpoints/snapshots/undo).
40. **PR / code-review automation** — Creating pull requests and/or automating code review of diffs/PRs.

### H. Interfaces
41. **CLI / headless mode** — Non-interactive/scriptable invocation (single-shot prompts, JSON/stream output, batch runs).
42. **Interactive TUI** — A rich interactive terminal UI (streaming render, dialogs, autocomplete, themes).
43. **IDE / editor extension** — Integration with editors (VS Code, JetBrains, Zed), natively or via ACP.
44. **Desktop GUI app** — A standalone native desktop/mobile application (Electron/Tauri/SwiftUI/etc.).
45. **Web UI / server / API** — A web interface and/or an HTTP server/API mode for remote control or embedding.
46. **Voice input / speech / TTS** — Speech-to-text input and/or text-to-speech output and voice interaction.

### I. Extensibility
47. **Custom slash commands** — A command system, especially user-defined commands (e.g. markdown templates with argument substitution).
48. **Hooks / lifecycle events** — Lifecycle event hooks (pre/post tool-use, session start/end, compaction, etc.) that can intercept/augment behavior.
49. **Plugin / extension ecosystem** — A pluggable extension system with a loader and (ideally) an installable package ecosystem.
50. **Skills system** — Loadable skill packages (e.g. SKILL.md) discovered and surfaced to / invocable by the model.
51. **Project rules / context files** — Discovery and injection of project instruction files (AGENTS.md, CLAUDE.md, .cursorrules, etc.).
52. **SDK / programmatic API** — A library/SDK or stable programmatic interface for embedding the agent in other software.

### J. Safety & Quality
53. **Permission / approval system** — Gating risky actions (shell, edits, network) behind confirmation, with allow/deny rules and grant scopes.
54. **Sandboxing / isolation** — Executing in isolated/sandboxed environments (containers, seatbelt/bubblewrap, SSH, network policy).
55. **Security review / scanning** — Built-in security review prompts or static/dependency scanning tooling (SAST, OSV, secret scanning).
56. **Test / lint runner integration** — Running tests/linters and feeding failures back into the loop (beyond LSP diagnostics).
57. **Telemetry / analytics** — Instrumentation for usage analytics/observability (OpenTelemetry, PostHog, etc.), with opt-out.
58. **Internationalization (i18n)** — Localization of the UI/output into multiple languages.

---

## Notes & Caveats

- **Scoring is comparative and source-based.** A `0` means no source evidence was found; it does not always mean the capability is impossible (e.g. local models often work via a generic OpenAI-compatible path even where a first-class integration is absent).
- **Different products, different goals.** `claude-code` is deliberately single-vendor (Anthropic) but extremely deep on safety/extensibility; `pi` is deliberately minimalist (no MCP/sub-agents/plan-mode/permissions by design, pushed to extensions); `openclaw`/`hermes` are broad multi-channel *platforms* whose "coding agent" is one facet; `aider` and `opencode` are focused terminal coding tools. Averages at the bottom of Table 1 are a rough aggregate only and weight all 58 features equally.
- **`kilocode` identity:** the reviewed code is the **Kilo CLI**, an Effect-TS fork of **OpenCode** (with VS Code/JetBrains/Zed front-ends), *not* the Roo-Code/Cline-lineage extension.
- **`claude-code` identity:** this is a full TypeScript/Bun implementation of the Claude Code CLI (custom Ink TUI, `query.ts`/`QueryEngine.ts` loop).
